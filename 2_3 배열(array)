/*
배열(array) - 순서가 있는 리스트
-배열의 특징
 * 배열은 문자 뿐만 아니라, 숫자, 객체, 함수 등고 포함할 수 있음
 * length: 배열의 길이 
 * push(): 배열 끝에 추가
 * pop(): 배열 끝 요소 제거, 여러요소를 한꺼번에 추가 가능
 * shift, unshift: 배열 앞에 제거(shift)/ 추가(unshift)
 * 반복문: for
   let days = ['월','화','수'];
   for(let index=0; index < days.length; index++){
    console.log(days[index])
   }
 * 반복문: for ... of 
   let days = ['월','화','수']; 
   for(let day of days){
   console.log(day)
   }   
*/
